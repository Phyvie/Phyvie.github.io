global_variables.css, reset.css and main.css are requirements to be added to each page to keep the layout consistent. 

The files in this folder are stylesheet that are used across multiple pages. 
Since they are purely for styling, but have no additional logic behind them, they are not listed as components in their own folder. 
