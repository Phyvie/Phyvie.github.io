BEM + styling:
Blocks => inner geometry (e.g. flex-direction, padding, gaps, ...)
Elements => outer geometry (e.g. margins)
Modifiers => manipulates blocks or elements geometry

s-- Styling = classes that are used to style blocks / elements; e.g. color, background, border, ...
